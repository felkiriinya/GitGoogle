import { VisitedRepoDirective } from './visited-repo.directive';

describe('VisitedRepoDirective', () => {
  it('should create an instance', () => {
    const directive = new VisitedRepoDirective();
    expect(directive).toBeTruthy();
  });
});
